
<h3>Upcomming Matches (6 months)</h1>
<div class="container">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Team1</th>
      <th>Team2</th>
      <th>Venue</th>
      <th>Schedule</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% upcomming_match.each do |match| %>
      <tr>
        <td><%= team_name_finder(match.team1_id) %></td>
        <td><%= team_name_finder(match.team2_id) %></td>
        <td><%= match.venue.location %></td>
        <td><%= match.schedule %></td>
        <td><%= link_to 'Match Details', match %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
